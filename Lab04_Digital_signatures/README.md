A digital signature is a cryptographic mechanism used to verify the authenticity and integrity of a message and provide non-repudiation.

A hash produces a representation of the message.
If the message changes, its hash changes.
Therefore, changing the message can cause signature verification to fail.


The signature is created using the signer's private key.
The corresponding public key can be used to verify the signature.